# Simple file to test out running Python scripts from the command line
# Have to invoke both the interpreter + the script in question

print("Howdy, this is hopefully coming from the cmd line!")

name = input("What's your name?: ")

print("Hello " + name)